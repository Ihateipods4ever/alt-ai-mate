[build]
  base = "packages/client"
  command = "npm install --no-audit --no-fund && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.17.1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200